# Werkmap van mykunda.com. Deze repo is sinds 25-08-2026 de bron;
# het Claude Design-project is dat niet meer. Zie CLAUDE.md.

# Niet meegespiegeld (grote originelen en oud materiaal)
uploads/
archief/
screenshots/
scraps/

# Door build.mjs gegenereerd - afleidbaar uit de bron, dus buiten de diff
deploy/
app.min.js
styles.min.css

# Tijdelijke bestanden van de Supabase CLI
supabase/.temp/

# Lokale Claude Code-instellingen voor deze repo
.claude/

# Terugdraai-kopieen van de patchscripts en de Overpass-cache: git is hier zelf
# al de geschiedenis, en de cache is opnieuw op te halen
_werk/backup-*/
_werk/osm-cache/
_werk/osm-log.txt

# Proefuitvoer van build-listing-pages.mjs (--uit=). Gegenereerd uit
# _werk/listing-fixtures.json en dus opnieuw te maken; hoort niet in de repo.
_werk/proef*/
_werk/indexnow.json

# Map met bestanden die weg mogen
_to_delete/
